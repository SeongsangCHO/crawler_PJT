import React from "react";
import PropTypes from "prop-types";

const LoginButton = (props) => {
  return <button>로그인이 필요합니다. 로그인 하시겠습니까?</button>;
};

LoginButton.propTypes = {};

export default LoginButton;
